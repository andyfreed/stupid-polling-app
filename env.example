#
# Supabase (recommended):
# - DATABASE_URL: pooled/transaction URL (best for Vercel serverless runtime)
# - DIRECT_URL: direct connection (recommended for Prisma migrations)
#
# Example names in Supabase dashboard:
# - "Transaction pooler" / "Connection pooling" -> DATABASE_URL
# - "Direct connection" -> DIRECT_URL
#
DATABASE_URL="postgresql://USER:PASSWORD@HOST:6543/postgres?pgbouncer=true&connection_limit=1"
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/postgres"

# Optional overrides
VOTEHUB_BASE_URL="https://api.votehub.com"
CIVICAPI_LATEST_URL="https://civicapi.org/api/v2/poll/latest"

# Ingestion tuning
INGEST_DAYS="30"
